{delete}
{require:serverAdmin}
**Admin Commands**
-`.superclean <amount>` (Clean bots :O)
-`.accept <@user> <what you accept>` (Accepts a request)
-`.deny <@user> <what you deny>` (denys a request)
-.commandunban <@user> <@role> (Allows a user to use commands)
-.demote <@user> <@role> (demotes from any role)
-.promote <@user> <@role> (Promotes a user to any role)
-Not all commands work yet```
